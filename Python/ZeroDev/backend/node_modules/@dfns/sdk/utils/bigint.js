"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.minimizeBigInt = void 0;
const minimizeBigInt = (value) => {
    const minValue = [0, ...value];
    for (let i = 0; i < minValue.length; ++i) {
        if (minValue[i] === 0) {
            continue;
        }
        if (minValue[i] > 0x7f) {
            return new Uint8Array(minValue.slice(i - 1));
        }
        return new Uint8Array(minValue.slice(i));
    }
    return new Uint8Array([0]);
};
exports.minimizeBigInt = minimizeBigInt;
